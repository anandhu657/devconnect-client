import { DatetoMinutesPipe } from './dateto-minutes.pipe';

describe('DatetoMinutesPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetoMinutesPipe();
    expect(pipe).toBeTruthy();
  });
});
